export type Phase12FixtureLocation = {
  candidateId: string;
  canonicalTarget: string;
  surface: string;
  phase: string;
  mode: string;
  dataKind: string;
  timezoneSemantics: string;
  location: string;
  status: string;
};
