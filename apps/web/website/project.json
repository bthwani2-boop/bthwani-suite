{
  "name": "website",
  "root": "apps/web/website",
  "sourceRoot": "apps/web/website/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vite --config apps/web/website/vite.config.ts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/apps/web/website"
      ],
      "options": {
        "command": "pnpm exec vite build --config apps/web/website/vite.config.ts"
      }
    }
  }
}