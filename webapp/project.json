{
  "name": "webapp",
  "root": "webapp",
  "sourceRoot": "webapp",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir webapp/runtime dev"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir webapp/runtime build"
      }
    }
  }
}
