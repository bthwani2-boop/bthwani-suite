# DSH Control-Panel Quarantine

- `operations-flow-legacy/` contains the old `_flow-id_` placeholder that was previously used as the live operations flow meta path.
- `partners-placeholder/` and `support-placeholder/` contain non-live section placeholders removed from the active DSH control-panel ownership model.
- The live DSH control-panel scope is now limited to `operations`, with canonical overview and child routes under `operations/dsh/`.