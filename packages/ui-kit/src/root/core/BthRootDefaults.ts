// Default values for root system
import { BthRootConfig } from './BthRootConfig';
import { directionConfig } from '../../foundation/direction';

export const BTH_ROOT_DEFAULTS: BthRootConfig = {
  direction: directionConfig.defaultDirection,
  language: directionConfig.defaultLanguage,
  themeMode: 'light',
};
