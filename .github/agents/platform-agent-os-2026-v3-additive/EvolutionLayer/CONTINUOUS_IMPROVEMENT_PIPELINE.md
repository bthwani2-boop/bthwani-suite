# Continuous Improvement Pipeline

The continuous loop is: execute task → capture evidence → evaluate quality → detect ui-kit/architecture/security/finance impacts → record memory → produce next action → optionally propose learning → run promotion gate.
