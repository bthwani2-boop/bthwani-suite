// Identity-access topic — roles, permissions, surface visibility per mode, audit.
// SSOT: files live in policies/ and contracts/ layers; this topic aggregates them.

export * from './dsh-role-permission.model';
export * from './surface-visibility.policy';
