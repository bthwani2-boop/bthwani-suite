# UI Kit Evolution Protocol

Every UI task must inspect ui-kit first, use existing primitives/components/patterns when available, register missing reusable patterns as candidates, and block local duplication when the pattern belongs centrally.
