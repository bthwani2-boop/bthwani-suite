{
  "name": "app-captain",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir apps/mobile/app-captain exec expo start --go --clear"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --dir apps/mobile/app-captain exec expo export --platform android"
      }
    }
  }
}
