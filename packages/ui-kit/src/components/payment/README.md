Payment components extracted from donor `auto_wlt_topup`.

Contains:
- `AmountInput` - numeric-only field wrapper around `BthTextField`
- `PaymentMethodList` - selectable provider cards
- `SummaryCard` - small key/value summary inside `BthCard`

These are intentionally minimal and intended to be extended.
