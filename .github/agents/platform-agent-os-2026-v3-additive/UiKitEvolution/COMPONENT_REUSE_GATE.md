# Component Reuse Gate

Before creating any local component, the agent must check ui-kit primitives/components/patterns/state. Existing central component must be reused. Missing central component must be registered as candidate before local implementation is accepted.
