# Execution Memory Registry Template

Each run must record: SESSION_ID, request, selected agents, scope, files inspected, evidence paths, result, blockers, repeated failures, ui-kit findings, next action, and whether the run produced a learning candidate.
