# Regression Memory Policy

A solved failure must be registered with its evidence and guard. If a later run repeats the same failure, the agent must label it REGRESSION and block expansion until the regression is resolved or explicitly waived by the human.
