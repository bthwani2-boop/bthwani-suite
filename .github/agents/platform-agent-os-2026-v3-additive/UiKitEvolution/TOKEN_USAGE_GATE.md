# Token Usage Gate

No arbitrary color, spacing, radius, shadow, typography, or motion value may be introduced when a token exists. Unverified hex values must be marked TBD. Brand direction is binding but exact values require evidence.
