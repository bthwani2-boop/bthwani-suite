# JOURNIES Large File and Performance Register

**Status before local script:** `TEMPLATE_REQUIRES_LOCAL_CENSUS`

| Path | Lines | Bytes | Risk reason | Split required? | Responsible slice | Later correction strategy |
|---|---:|---:|---|---|---|---|
| _run local census_ | 0 | 0 | `NEEDS_GIT_EVIDENCE` | pending | J-011 | run census script |

Risk classes include: `mixed ownership`, `too many responsibilities`, `UI + state + API + data in one file`, `N+1`, `overfetch`, `polling`, `missing pagination`, `missing indexes`, `local expensive visual recipes`.
