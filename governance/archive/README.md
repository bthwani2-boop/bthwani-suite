# governance/archive/

This folder contains archived or legacy governance artifacts. Files moved here are preserved for provenance and evidence but are not active authority.

Rules:
- Do not treat files under `governance/archive/` as active governance authority.
- Each archived file must have a JSON companion entry under `_handoff/` describing `original_path`, `sha256_original`, `reason`, and `archivedAt`.
