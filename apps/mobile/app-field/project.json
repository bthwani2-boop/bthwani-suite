{
  "name": "app-field",
  "root": "apps/mobile/app-field",
  "sourceRoot": "apps/mobile/app-field/src",
  "projectType": "application",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm exec vite --config apps/mobile/app-field/vite.config.ts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": [
        "{workspaceRoot}/dist/apps/mobile/app-field"
      ],
      "options": {
        "command": "pnpm exec vite build --config apps/mobile/app-field/vite.config.ts"
      }
    },
    "expo": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pwsh -File tools/scripts/start-mobile-preview.ps1 -App app-field -Mode lan -Clear"
      }
    },
    "expo-tunnel": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pwsh -File tools/scripts/start-mobile-preview.ps1 -App app-field -Mode tunnel -Clear"
      }
    }
  }
}