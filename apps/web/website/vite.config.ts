import { createSurfaceAppViteConfig } from '../../../tools/vite/create-surface-app-config';

export default createSurfaceAppViteConfig({
  appRoot: 'apps/web/website',
  surfaceId: 'website',
});